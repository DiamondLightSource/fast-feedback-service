{{- if .Values.job.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  backoffLimit: {{ .Values.job.backoffLimit }}
  ttlSecondsAfterFinished: {{ .Values.job.ttlSecondsAfterFinished }}
  template:
    metadata:
      name: {{ .Release.Name }}
      labels:
        app: {{ .Release.Name }}
    spec:
      restartPolicy: Never
      {{- if .Values.podSecurityContext }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- end }}
      containers:
        - image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: Always
          name: {{ .Release.Name }}
          command: ["/opt/ffs/bin/ffs_index_integrate"]
          args:
            - "--reflection"
            - {{ .Values.job.reflection | quote }}
            - "--experiment"
            - {{ .Values.job.experiment | quote }}
            - "--working-directory"
            - {{ .Values.job.workingDirectory | quote }}
            - "--max-cell"
            - {{ .Values.job.maxCell | quote }}
            {{- if .Values.job.dmin }}
            - "--dmin"
            - {{ .Values.job.dmin | quote }}
            {{- end }}
            {{- range .Values.job.extraArgs }}
            - {{ . | quote }}
            {{- end }}
          {{- if .Values.extraEnv }}
          env:
            {{- range $key, $value := .Values.extraEnv }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          {{- end }}
          {{- include "ffs.resources" . | nindent 10 }}
          {{- if .Values.containerSecurityContext }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          {{- end }}
          {{- include "ffs.volumeMounts" (dict "Values" .Values "readOnlyDls" false) | nindent 10 }}
      {{- include "ffs.nodeSelector" . | nindent 6 }}
      {{- include "ffs.tolerations" . | nindent 6 }}
      {{- include "ffs.volumes" . | nindent 6 }}
{{- end }}
